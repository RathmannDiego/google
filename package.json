{
  "name": "google",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "deploy:dev": "tsc && wrangler deploy --env dev",
    "deploy:qa": "tsc && wrangler deploy --env qa",
    "deploy:prd": "tsc && wrangler deploy --env prd"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}

