{
  "name": "google",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "deploy": "node update-git-info.js && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260702.1",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}